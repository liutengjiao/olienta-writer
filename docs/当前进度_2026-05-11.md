# Olienta 当前进度记录

日期：2026-05-11

## 本轮完成

- 修正本地 Markdown 页面：打开无痛剥离测试项目后，可以列出并读取 `framework/01-setting.md` 到 `framework/06-style.md` 的真实内容。
- 本地 Markdown 默认用文档预览方式显示，不再显示“浏览器预览模式”的占位文字。
- 本地 Markdown 页面保留“编辑 / 预览”切换；编辑模式用于查看 Markdown 源码，正式修改仍回到对应模块，避免绕过作者确认、蓝图覆盖和事实抽取规则。
- 中间工作区改为桌面软件固定框架：页面本体不无限向下增长，滚动发生在中间工作区内部。
- 窄窗口或右侧助手打开时，本地 Markdown 文件列表会改为上方列表、下方文档，避免挤压正文阅读宽度。
- 顶部栏清理为品牌和当前项目名，不再放全局工具按钮或右上角状态工具。
- “导入 / 导出”移动到每个页面标题右侧，和“编辑 / 预览 / 保存 / AI 生成”等页面动作放在同一层。
- 新增最左侧窄模块导航栏，上方入口为：首页、项目结构、知识库、角色；下方入口为：任务、日志、模型调用。
- 修正窄模块导航栏的链接逻辑：窄栏现在先切换一级模块，不再直接跳到项目结构树里的页面；原来的小说设置、故事构架、章节蓝图、草稿箱、正文全部归入“项目结构”模块。
- 新增模块框架说明文档：`docs/模块框架栏.md`。
- 为知识库、角色、任务、日志、模型调用补齐模块二级栏目和对应中间展示页。

## 已验证

- `npm run build` 通过。
- `npm run lint` 通过。
- `cargo test` 通过，26 项测试全部通过。
- 自动页面测试通过：
  - 打开无痛剥离测试项目。
  - 进入“本地 Markdown”。
  - 点击 `framework/02-premise.md`。
  - 页面出现“1号文件：故事前提”“一句话前提”和 2017 相关正文。
  - 页面不再出现“浏览器预览模式”占位文字。
  - 浏览器页面本体不滚动，中间工作区内部滚动。
- 自动页面结构检查通过：
  - 最左侧窄栏出现 7 个入口：首页、项目结构、知识库、角色、任务、日志、模型调用。
  - 顶部栏不再包含按钮。
  - 当前页面标题动作区包含“导入 / 导出”。
  - 左窄栏、项目结构栏、中间工作区、任务栏的位置关系符合桌面框架。
- 自动模块切换检查通过：
  - 点击“首页 / 知识库 / 角色 / 模型调用”进入各自模块首页和模块第二栏。
  - 点击“项目结构”才回到小说设置、故事构架、章节蓝图等原项目树。
- 自动二级栏目检查通过：
  - 知识库：事实库、语义检索可切换到对应页面。
  - 角色：角色列表、关系图谱可切换到对应页面。
  - 模型调用：调用记录可切换到对应页面。

## 下一步

- 继续把故事构架各二级栏目做成更接近样板的纯 Markdown 文档阅读体验。
- 检查章节蓝图、草稿箱、正文三类章节二级栏目是否都严格对应真实本地文件。
- 继续收紧右侧助手：默认不抢占主工作区，只有需要 AI 或候选稿时展开。

## 本次补充：本地文件骨架

- 明确 Olienta 第一版不使用数据库；小说项目就是本地文件夹。
- `create_project` 和 `open_project` 现在会补齐更完整的普通文件层级：`knowledge/`、`characters/`、`tasks/`、`logs/`、`models/`、`timeline/milestones.md`、`facts/forbidden-rules.md`。
- 保留现有路径：`framework/`、`blueprints/`、`manuscript/`、`facts/`、`skills/selected/`、`exports/`、`.olienta/`，避免破坏已完成页面。
- 重写 `docs/LOCAL_PROJECT_MODEL.md` 为中文，说明每个目录对应的软件模块和作者可直接查看的文件。

## 本次补充：窄栏模块接入本地文件

- “知识库 / 本地 Markdown”现在直接复用真实 Markdown 文件浏览器，不再只是说明页。
- “知识库 / Skill”现在直接复用 Skill 管理面板，可看到导入、启用、停用、临时启用和文件预览。
- “模型调用 / AI Provider”现在可直接显示并编辑 `.olienta/ai-providers.json`。
- “角色、任务、日志、模型调用”等模块页面增加“本地文件”清单，明确每个页面背后对应的项目文件。
- 前端构建和代码检查通过。

## 本次补充：模块 Markdown 阅读入口

- “角色 / 总览、角色列表、关系图谱、成长线”现在提供对应 Markdown 文件的直接打开预览。
- “知识库 / 总览、语义检索”现在可以打开 `knowledge/` 与 `facts/` 下的说明和事实文件。
- “日志 / 作者确认日志、系统事件”现在可以打开作者确认记录和蓝图级联记录。
- “模型调用 / 调用记录、连接测试”现在可以打开模型调用记录说明文件。
- 这些入口目前先只读，避免绕过作者确认、正文保存、事实抽取和蓝图级联规则；下一步按模块补保存。

## 本次补充：模块辅助文档保存

- 模块 Markdown 预览现在支持“编辑 / 预览”切换。
- 允许直接保存的范围只限模块辅助文档：`knowledge/`、`characters/`、`logs/author-confirmation.md`、`logs/model-calls/README.md`、`models/README.md`。
- 明确禁止从这个入口保存正文、蓝图、故事构架和事实库，避免绕过作者确认、事实抽取和蓝图级联。
- 新增后端测试，确认 `characters/relations.md` 可以保存，而 `manuscript/chapters/001.md` 会被拒绝。
