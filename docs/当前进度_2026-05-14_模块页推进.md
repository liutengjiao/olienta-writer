# 当前进度 2026-05-14：模块页推进

## 本轮完成

- 首页补成可操作入口：显示当前项目、打开本地项目、打开无痛剥离测试项目、最近项目和导出入口。
- 知识库增加“本地文档检索”雏形：第一版先检索项目内可见文件名和分类，后续再接本地向量索引。
- 日志增加作者确认日志工作面：可读取确认边界、确认日志和候选稿采用记录。
- 模型调用增加连接测试工作面：集中显示 Provider 测试结果、测试按钮和调用记录预览。
- 保留所有内容本地文件优先：这些页面只读取或预览项目文件夹中的 Markdown、JSON、JSONL，不引入数据库。

## 验证

- `npm run lint` 通过。
- `npm run build` 通过。
- 浏览器中实际点击验证：首页、知识库、语义检索、日志、模型调用、连接测试页面均能显示对应内容。

## 下一步

- 把角色模块继续做实：角色卡列表、单角色编辑、关系图谱、成长线。
- 把知识库检索从“文件名检索”升级到“文件内容检索”，再为未来本地向量索引留接口。
- 把任务页继续细化为任务队列、任务历史和失败重试。
