# 当前进度 2026-05-14：角色档案摘要

## 本轮完成

- 角色卡详情页增加“人物档案”摘要区。
- 打开 `characters/cards/*.md` 后，会从 Markdown 中提取：
  - 姓名
  - 定位
  - 欲望
  - 恐惧
  - 关系线索
  - 不可误写边界
- 摘要只用于帮助作者快速扫读，不替代原始 Markdown。作者仍然可以逐字编辑并保存角色卡。
- 候选稿审查的角色边界关键词扩展到关系图谱和成长线：
  - 关系
  - 利益
  - 压迫
  - 冲突
  - 成长
  - 状态
  - 转折

## 验证

- `npm run lint` 通过。
- `npm run build` 通过。
- `cargo test` 通过，33 个后端测试全部通过。

## 下一步

- 给角色卡增加更明确的结构化字段模板，让抽取后的角色卡更稳定。
- 角色模块继续增加“关系边”列表和“成长线节点”列表，为后续可视化做准备。
- 候选稿审查继续升级为按角色分组显示风险，而不是只给一串平铺提醒。
