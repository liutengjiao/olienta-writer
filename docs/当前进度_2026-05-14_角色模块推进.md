# 当前进度 2026-05-14：角色模块推进

## 本轮完成

- 角色模块增加“角色列表”工作面：
  - 从 `characters/cards/*.md` 读取角色卡。
  - 支持打开 `characters/cards/README.md`。
  - 支持从 `framework/03-characters.md` 抽取角色卡。
  - 角色卡可以在模块内编辑和保存。

- 角色模块增加“关系图谱”工作面：
  - 默认读取 `characters/relations.md`。
  - 用 Markdown 编辑角色关系、欲望、利益、压迫链和冲突边。
  - 保存后仍然落到本地 Markdown 文件。

- 角色模块增加“成长线”工作面：
  - 默认读取 `characters/growth.md`。
  - 用 Markdown 编辑角色状态随章节变化的记录。
  - 后续可和章节蓝图、事实库、时间轴一起做一致性校验。

- 避免重复页面：
  - 角色列表、关系图谱、成长线、语义检索、作者确认日志、连接测试这些已经有专门页面的二级栏目，不再重复显示通用“文档预览”面板。

## 验证

- `npm run lint` 通过。
- `npm run build` 通过。

## 下一步

- 给角色卡增加更清楚的结构化摘要，例如定位、首次出场、核心欲望、禁止误写事实。
- 把关系图谱从纯 Markdown 进一步升级为可视化关系边。
- 把成长线接入候选稿审查，让 AI 写作时检查角色状态是否提前变化或自相矛盾。
