# 当前进度：项目作品库清单

日期：2026-05-14

本轮把首页“外部作品文件夹地图”从前端临时汇总升级为后端真实扫描的作品库清单。

## 已完成

- 新增后端命令：`list_project_vault_entries`。
- 扫描项目文件夹中的普通作品文件：
  - Markdown：`.md`、`.markdown`
  - 文本与配置：`.txt`、`.json`、`.jsonl`、`.yaml`、`.yml`
  - 导出成品：`.docx`
- 每个文件返回：
  - 分类
  - 项目内相对路径
  - 文件体积
  - 扩展名
  - 是否可在 Olienta 内直接预览
- 首页文件夹地图改为读取这个真实清单，不再只依赖 Markdown 列表。
- `.docx` 等不可直接预览文件会以文件标签显示，不会误导为可编辑 Markdown。

## 分类规则

- `framework/`：故事构架
- `blueprints/chapters/`：章节蓝图
- `manuscript/chapters/`：正文
- `manuscript/candidates/`：草稿箱
- `facts/`：事实库
- `timeline/`：时间轴
- `skills/`：Skill
- `tasks/`：任务
- `logs/`：日志
- `models/`：模型调用
- `exports/`：导出
- `.olienta/`：项目配置
- `.olienta-events/`：保存事件

## 验证

- `cargo test`：40 个测试全部通过。
- `npm run lint`：通过。
- `npm run build`：通过。

