# 当前进度：2026-05-15 接手推进总控

本文件记录从另一台电脑复制项目后，本机接手继续推进的最新状态。后续继续开发时，优先读本文件，再按需要回看 `docs/` 下更细的模块文档。

## 当前仓库状态

- 当前分支：`master`。
- 当前已有可回退基线和多轮 UI 文案清理提交。
- 最近检查时 `git status --short` 为空，工作区干净。
- 项目主体位于 `app/`，技术栈为 React + TypeScript + Vite + Tauri 2 + Rust。
- 本机开发服务使用 Vite 端口 `1420`，当前可访问：`http://localhost:1420`。本机这次监听在 IPv6 回环，`127.0.0.1:1420` 可能连不上。

## 本轮接手后已完成

### 1. 模块页从文件列表推进为专用工作区

`app/src/components/Workspace.tsx` 已新增或重建以下专用页面：

- `KnowledgePanel`：知识库总览、项目健康、资料导入、结构修复入口。
- `KnowledgeFactsPanel`：已确认事实与未闭合伏笔的双文档编辑。
- `KnowledgeSearchPanel`：本地全文检索、范围筛选、单条/批量钉选进任务书、查看/移除已钉材料。
- `CharactersPanel`：角色总览、角色卡、关系图谱、成长线和角色卡抽取入口。
- `TasksPanel`：当前章节任务书工作台、任务状态、钉选材料查看/移除、生成候选稿入口。
- `DraftPanel`：候选稿审查台，支持候选稿编辑、保存、清空、历史预览/恢复、追加/替换进入正文。
- `ManuscriptPanel`：作者确认正文工作台，支持保存确认、事实重扫、当前章导出、纯写作模式入口。
- `LocalFilesPanel`：本地 Markdown 分类浏览、打开、定位、编辑保存。
- `SkillPanel`：Skill 导入、启用/停用、临时启用、预览。
- `ModelCallsPanel`：模型调用记录、Provider 测试和历史入口。

### 2. 已串起的核心写作链路

当前主链路已经比较完整：

```text
本地资料导入 / 本地 Markdown
  -> 本地全文检索
  -> 检索结果钉选进当前章节任务书
  -> 任务书工作台编辑和装配
  -> 生成候选稿
  -> 候选稿审查、历史恢复、追加或替换
  -> 正文页作者确认保存
  -> 事实库重扫、作者确认日志、章节导出
```

### 3. 产品原则继续保留

- 本地 Markdown 文件仍是写作项目的源头。
- AI 输出只进入候选稿，不自动进入正文。
- 正文保存才视为作者确认。
- 候选稿进入正文必须由作者手动选择追加或替换。
- 事实库、伏笔、任务书、日志都仍是普通本地文件，可直接查看。

### 4. 高频界面文案已开始清理

已完成第一轮高频区域可读性修复：

- 重写 `ProjectPanel.tsx`：全局导航、项目结构树、模块入口、章节树和最近项目文案改为清晰中文。
- 重写 `AgentPanel.tsx`：右侧上下文助手、生成前检查、候选稿操作、框架/知识库/Provider 折叠区改为清晰中文。
- 清理 `TopBar.tsx`：未打开项目状态文案。
- 清理 `Taskbar.tsx`：底部任务栏和空状态文案。
- 清理 `constants.ts`：默认章节、项目名、模板和任务状态文案。
- 清理 `Workspace.tsx` 第一轮：页面标题、项目入口、草稿箱、正文、知识库、检索、任务、日志、模型调用、导出等主要可见文案。

对应提交：

- `cf5e4f0 checkpoint: establish Olienta rebuild baseline`
- `75e7ac7 fix: clean primary navigation and assistant copy`
- `a3be75b docs: update handoff progress after UI copy cleanup`
- `d28710e fix: clean workspace visible copy`

### 5. 操作状态文案已复核

已复核以下 hook 的高频操作反馈，当前基本是可读中文：

- `useProjectLifecycle.ts`：创建、打开、选择文件夹、浏览器预览提示。
- `useChapterWorkspace.ts`：章节读取、保存、预览模式、未保存状态。
- `useProjectResources.ts`：知识库、资料导入、Skill、时间线、框架文件、AI Provider 状态提示。

### 6. `Workspace.tsx` 已完成主拆分

已把四组成熟页面从主工作区文件中拆出：

- `app/src/components/workspace/EditorPanels.tsx`：草稿箱、正文确认、通用 Markdown 编辑器、章节列表、纯写作模式。
- `app/src/components/workspace/DocumentPanels.tsx`：本地 Markdown、Skill、任务书、日志、模型调用、导出。
- `app/src/components/workspace/KnowledgePanels.tsx`：知识库总览、事实/伏笔、全文检索、项目健康、角色工作台。
- `app/src/components/workspace/CorePanels.tsx`：首页入口、小说设置、框架文件、章节蓝图、页面外壳。

`Workspace.tsx` 现在只保留路由标题和路由分发。共享 `WorkspaceProps` 与标题常量已移动到 `app/src/components/workspace/types.ts`，拆出的面板不再依赖主组件文件。

### 7. 已补轻量 dev server smoke 检查

新增 `app/scripts/dev-smoke.mjs` 和 `npm run smoke:dev`。在没有 Playwright / agent-browser 的情况下，该脚本会检查：

- dev server HTML 是否返回 200。
- HTML 是否包含 `#root` 挂载点。
- Vite 入口资源、客户端脚本和 favicon 是否可访问。
- HTML 中是否出现 Vite overlay 标记。

### 8. 已补写作主链路 Rust 自动测试

新增 `writing_flow_keeps_candidate_author_gated_until_confirmed_save`，覆盖：

- 钉选本地检索材料进章节任务书。
- 装配写作任务书并确认钉选材料进入上下文。
- 生成候选稿，并确认候选稿不会自动进入正文。
- 记录候选稿采用确认。
- 保存正文后进入作者确认链。
- 导出当前章并检查系统事件与任务历史。

新增 `search_scope_batch_pin_and_remove_recompose_writing_brief`，覆盖：

- 分范围检索导入资料、框架文件和正文链路。
- 批量钉选检索结果进章节任务书。
- 移除钉选材料后重新装配任务书。
- 检查任务历史写入批量钉选和移除事件。

### 9. 已补一键验证命令

新增 `app/scripts/verify.mjs` 和 `npm run verify`，按顺序执行：

- `npm run build`
- `npm run lint`
- `npm run smoke:dev`
- `cargo test`

`npm run verify` 会复用已有 `http://localhost:1420` dev server；如果没有检测到服务，会临时启动 `npm run dev`，完成 smoke 后关闭自己启动的进程。

## 已验证

最近一次验证结果：

```powershell
cd app
npm run verify
```

结果：

- `npm run build` 通过。
- `npm run lint` 通过。
- `npm run smoke:dev` 通过，9 项检查全部通过。
- `cargo test` 通过，13 个测试全部通过。
- `npm run verify` 通过，已串起前端构建、lint、smoke 和 Rust 测试。
- `http://localhost:1420` 返回 200。
- 本轮尝试使用 `agent-browser` 做页面自动化验证，但本机当前没有暴露该命令；已先用构建、lint、Rust 单测和 HTTP 可达性覆盖结构调整风险。

Rust 侧仍有两个既有 warning：

- `AiProviderConfig` 部分字段暂未读取。
- `extract_markdown_section` 暂未使用。

这两个 warning 不影响当前功能和测试。

## 当前主要风险

1. 前端显示文本仍可能有低频历史乱码  
   高频区已清理：主导航、顶部栏、任务栏、右侧助手、工作区主要页面、默认常量和核心 hook 状态消息。后续需要在真实点击流中继续扫低频弹窗、错误分支和样例内容。

2. 浏览器级自动化验证不足  
   已新增无浏览器 smoke 检查和自动 dev server 管理。本机仍未暴露 `agent-browser`，Node 环境也缺少 Playwright，尚未做完整点击流截图验证。

3. `WorkspaceProps` 仍较宽  
   类型已独立到 `workspace/types.ts`。后续如果继续组件化，可按页面收窄 props，减少每个面板能访问的无关字段。

## 建议下一步

优先级从高到低：

1. 做真实点击流验证  
   smoke 只能验证入口资源，不验证 React 交互。后续应覆盖打开样例项目、知识库检索、钉选材料、任务书、候选稿、正文保存。

2. 继续扫低频文案和错误分支  
   用 `rg` 和真实点击流结合检查残留乱码，优先处理用户能看到的消息。

3. 强化 Word 导出和正文排版  
   当前导出已可用，后续需要模板、页眉页脚、章节样式等。

## 当前接力入口

如果继续开发，建议从以下文件开始：

- `app/src/components/Workspace.tsx`
- `app/src/components/workspace/EditorPanels.tsx`
- `app/src/components/workspace/DocumentPanels.tsx`
- `app/src/components/workspace/KnowledgePanels.tsx`
- `app/src/components/workspace/CorePanels.tsx`
- `app/src/components/workspace/types.ts`
- `app/scripts/dev-smoke.mjs`
- `app/scripts/verify.mjs`
- `app/src/App.tsx`
- `app/src/hooks/useProjectResources.ts`
- `app/src/hooks/useChapterWorkspace.ts`
- `app/src-tauri/src/project_model.rs`

如果先做版本管理，建议先运行：

```powershell
git status --short
```

提交前可再次运行：

```powershell
cd app
npm run verify
```
