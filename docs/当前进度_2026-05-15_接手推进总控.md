# 当前进度：2026-05-15 接手推进总控

本文件记录从另一台电脑复制项目后，本机接手继续推进的最新状态。后续继续开发时，优先读本文件，再按需要回看 `docs/` 下更细的模块文档。

## 当前仓库状态

- 当前分支：`master`。
- 当前已有可回退基线和多轮 UI 文案清理提交。
- 最近检查时 `git status --short` 为空，工作区干净。
- 项目主体位于 `app/`，技术栈为 React + TypeScript + Vite + Tauri 2 + Rust。
- 本机开发服务使用 Vite 端口 `1420`，当前可访问：`http://localhost:1420`。本机这次监听在 IPv6 回环，`127.0.0.1:1420` 可能连不上。

## 本轮接手后已完成

### 1. 模块页从文件列表推进为专用工作区

`app/src/components/Workspace.tsx` 已新增或重建以下专用页面：

- `KnowledgePanel`：知识库总览、项目健康、资料导入、结构修复入口。
- `KnowledgeFactsPanel`：已确认事实与未闭合伏笔的双文档编辑。
- `KnowledgeSearchPanel`：本地全文检索、范围筛选、单条/批量钉选进任务书、查看/移除已钉材料。
- `CharactersPanel`：角色总览、角色卡、关系图谱、成长线和角色卡抽取入口。
- `TasksPanel`：当前章节任务书工作台、任务状态、钉选材料查看/移除、生成候选稿入口。
- `DraftPanel`：候选稿审查台，支持候选稿编辑、保存、清空、历史预览/恢复、追加/替换进入正文。
- `ManuscriptPanel`：作者确认正文工作台，支持保存确认、事实重扫、当前章导出、纯写作模式入口。
- `LocalFilesPanel`：本地 Markdown 分类浏览、打开、定位、编辑保存。
- `SkillPanel`：Skill 导入、启用/停用、临时启用、预览。
- `ModelCallsPanel`：模型调用记录、Provider 测试和历史入口。

### 2. 已串起的核心写作链路

当前主链路已经比较完整：

```text
本地资料导入 / 本地 Markdown
  -> 本地全文检索
  -> 检索结果钉选进当前章节任务书
  -> 任务书工作台编辑和装配
  -> 生成候选稿
  -> 候选稿审查、历史恢复、追加或替换
  -> 正文页作者确认保存
  -> 事实库重扫、作者确认日志、章节导出
```

### 3. 产品原则继续保留

- 本地 Markdown 文件仍是写作项目的源头。
- AI 输出只进入候选稿，不自动进入正文。
- 正文保存才视为作者确认。
- 候选稿进入正文必须由作者手动选择追加或替换。
- 事实库、伏笔、任务书、日志都仍是普通本地文件，可直接查看。

### 4. 高频界面文案已开始清理

已完成第一轮高频区域可读性修复：

- 重写 `ProjectPanel.tsx`：全局导航、项目结构树、模块入口、章节树和最近项目文案改为清晰中文。
- 重写 `AgentPanel.tsx`：右侧上下文助手、生成前检查、候选稿操作、框架/知识库/Provider 折叠区改为清晰中文。
- 清理 `TopBar.tsx`：未打开项目状态文案。
- 清理 `Taskbar.tsx`：底部任务栏和空状态文案。
- 清理 `constants.ts`：默认章节、项目名、模板和任务状态文案。
- 清理 `Workspace.tsx` 第一轮：页面标题、项目入口、草稿箱、正文、知识库、检索、任务、日志、模型调用、导出等主要可见文案。

对应提交：

- `cf5e4f0 checkpoint: establish Olienta rebuild baseline`
- `75e7ac7 fix: clean primary navigation and assistant copy`
- `a3be75b docs: update handoff progress after UI copy cleanup`
- `d28710e fix: clean workspace visible copy`

### 5. 操作状态文案已复核

已复核以下 hook 的高频操作反馈，当前基本是可读中文：

- `useProjectLifecycle.ts`：创建、打开、选择文件夹、浏览器预览提示。
- `useChapterWorkspace.ts`：章节读取、保存、预览模式、未保存状态。
- `useProjectResources.ts`：知识库、资料导入、Skill、时间线、框架文件、AI Provider 状态提示。

### 6. `Workspace.tsx` 已完成主拆分

已把四组成熟页面从主工作区文件中拆出：

- `app/src/components/workspace/EditorPanels.tsx`：草稿箱、正文确认、通用 Markdown 编辑器、章节列表、纯写作模式。
- `app/src/components/workspace/DocumentPanels.tsx`：本地 Markdown、Skill、任务书、日志、模型调用、导出。
- `app/src/components/workspace/KnowledgePanels.tsx`：知识库总览、事实/伏笔、全文检索、项目健康、角色工作台。
- `app/src/components/workspace/CorePanels.tsx`：首页入口、小说设置、框架文件、章节蓝图、页面外壳。

`Workspace.tsx` 现在只保留路由标题和路由分发。共享 `WorkspaceProps` 与标题常量已移动到 `app/src/components/workspace/types.ts`，拆出的面板不再依赖主组件文件。

### 7. 已补轻量 dev server smoke 检查

新增 `app/scripts/dev-smoke.mjs` 和 `npm run smoke:dev`。在没有 Playwright / agent-browser 的情况下，该脚本会检查：

- dev server HTML 是否返回 200。
- HTML 是否包含 `#root` 挂载点。
- Vite 入口资源、客户端脚本和 favicon 是否可访问。
- HTML 中是否出现 Vite overlay 标记。

### 8. 已补写作主链路 Rust 自动测试

新增 `writing_flow_keeps_candidate_author_gated_until_confirmed_save`，覆盖：

- 钉选本地检索材料进章节任务书。
- 装配写作任务书并确认钉选材料进入上下文。
- 生成候选稿，并确认候选稿不会自动进入正文。
- 记录候选稿采用确认。
- 保存正文后进入作者确认链。
- 导出当前章并检查系统事件与任务历史。

新增 `search_scope_batch_pin_and_remove_recompose_writing_brief`，覆盖：

- 分范围检索导入资料、框架文件和正文链路。
- 批量钉选检索结果进章节任务书。
- 移除钉选材料后重新装配任务书。
- 检查任务历史写入批量钉选和移除事件。

### 9. 已补一键验证命令

新增 `app/scripts/verify.mjs` 和 `npm run verify`，按顺序执行：

- `npm run build`
- `npm run lint`
- `npm run smoke:dev`
- `cargo test`

`npm run verify` 会复用已有 `http://localhost:1420` dev server；如果没有检测到服务，会临时启动 `npm run dev`，完成 smoke 后关闭自己启动的进程。

### 10. Rust 验证输出已清理

已处理 Rust 侧长期 warning：

- `AiProviderConfig` 的 `kind`、`baseUrl`、`model`、`temperature` 现在参与 Provider 状态和占位调用信息。
- 移除未使用的 `extract_markdown_section` helper。

## 已验证

最近一次验证结果：

```powershell
cd app
npm run verify
```

### 11. 后端高频可见文案继续清理

本轮清理 `app/src-tauri/src/project_model.rs` 中会直接影响用户体验和 AI 输出的乱码文案：

- Provider 测试提示和无可用 Provider 消息。
- Skill 冲突检测、已选 Skill 展示、角色上下文和 Timeline Pro 状态说明。
- 角色卡抽取模板，包括身份、欲望、恐惧、边界、标志物等字段。
- 事实重扫输出、章节事实、作者确认链。
- 候选稿审查提示，包括蓝图禁区、必须发生、事实约束、角色约束、钉选材料、时间线提前触发等。
- 框架文档和章节蓝图的 AI 提示词、本地 fallback 草稿、后续蓝图级联记录。

本轮已重新执行：

```powershell
cd app
npm run verify
```

结果：前端 build、lint、dev smoke、13 个 Rust 测试全部通过。

### 12. 候选稿审查分组与核心工作区文案复核

本轮继续推进草稿箱/正文主链路：

- 复核并清理 `Workspace.tsx`、`CorePanels.tsx`、`EditorPanels.tsx`、`workspace/types.ts` 的核心可见文案。
- 候选稿审查清单从平铺列表升级为按风险来源分组展示：
  - 时间线与里程碑
  - 章节蓝图
  - 事实库与禁写规则
  - 角色边界
  - 钉选材料
  - 伏笔与回收
  - 生成与任务书
  - 文本质量
- 本地审查报告 `manuscript/candidates/reviews/{chapter}.md` 同步改为中文标题和分组 Markdown，离开界面也能直接查看风险来源。
- Rust 写作主链路测试补充审查报告分组断言。

本轮已重新执行：

```powershell
cd app
npm run verify
```

结果：前端 build、lint、dev smoke、13 个 Rust 测试全部通过。

### 13. 知识库、任务、日志与角色工作台文案复核

本轮继续清理高频工作页：

- 重写 `DocumentPanels.tsx` 的可见文案，覆盖本地 Markdown、Skill、任务书、钉选材料、日志、模型调用和导出面板。
- 重写 `KnowledgePanels.tsx` 的可见文案，覆盖知识库总览、本地全文检索、批量钉选、项目健康、事实库、伏笔和角色工作台。
- 保留原有组件结构、CSS 类名、props 回调和数据流，只处理展示文案和状态提示。
- 对 `app/src/components`、`app/src/hooks`、`constants.ts`、`types.ts`、`utils.ts` 做乱码扫描，当前未发现典型 mojibake 命中。

本轮已重新执行：

```powershell
cd app
npm run verify
```

结果：前端 build、lint、dev smoke、13 个 Rust 测试全部通过。

### 14. 后端落盘模板中文化

本轮继续清理会写入用户项目文件夹的后端模板：

- 写作任务书改为中文标题、边界、输出位置和上下文分区。
- 钉选检索材料文件改为中文标题和说明。
- 本地候选稿 fallback 改为中文标题、说明和降级原因。
- 新项目默认章节、草稿、作者输入和章节蓝图模板改为中文。
- 角色卡索引、关系图谱、角色成长线默认内容改为中文。
- 同步调整 Rust 测试断言，确保写作主链路继续覆盖候选稿、钉选材料、审查报告和导出。

本轮已重新执行：

```powershell
cd app
npm run verify
```

结果：前端 build、lint、dev smoke、13 个 Rust 测试全部通过。

### 15. 文案乱码回归检查接入 verify

本轮把前面多轮文案清理沉淀成自动校验：

- 新增 `app/scripts/check-copy.mjs`。
- 新增 `npm run check:copy`。
- `npm run verify` 现在会先执行 copy check，再执行前端 build、lint、dev smoke 和 Rust 测试。
- 扫描范围覆盖 `app/src`、`app/src-tauri/src` 和 `docs`，跳过构建产物、依赖目录和样例正文。
- 修复 copy check 首次发现的 3 个后端落盘残留：蓝图级联来源、选中章节导出标题、空正文占位判断。
- 更新 `docs/BUILD_AND_RUN.md`，补充 copy check 与完整验证说明。

本轮已重新执行：

```powershell
cd app
npm run verify
```

结果：copy check、前端 build、lint、dev smoke、13 个 Rust 测试全部通过。

### 16. 后端低频落盘文案补漏

本轮继续处理 copy check 初版没有覆盖到的低频后端文案：

- 修复章节列表中空章节状态的乱码，统一显示为“待写”。
- 修复分类事实文件在无自动抽取内容时的中文占位。
- 分类事实文件标题从英文改为中文：角色事实、时间事实、地点事实、关系事实、世界规则、事件事实。
- 扩展 `app/scripts/check-copy.mjs` 的 mojibake 特征库，补上空章节状态和空事实占位相关的漏网组合。
- 补充 Rust 断言，覆盖章节状态、分类事实中文标题和空分类占位，防止后续退回乱码。

本轮已重新执行：

```powershell
cd app
npm run check:copy
cd src-tauri
cargo test
```

结果：copy check 通过，13 个 Rust 测试全部通过。

### 17. Dev smoke 升级为前端源模块契约检查

本轮把 dev smoke 从“只检查 HTML 和资源可达”升级为更接近前端入口验证的检查：

- `app/index.html` 语言改为 `zh-CN`，标题改为 `Olienta Writer`。
- `app/scripts/dev-smoke.mjs` 新增 HTML 语言和标题检查。
- `dev-smoke` 会继续检查 Vite 入口资源可达，以及 error overlay 标记未出现。
- 新增 6 个关键前端源模块契约检查：`App.tsx`、`ProjectPanel.tsx`、`Workspace.tsx`、`CorePanels.tsx`、`DocumentPanels.tsx`、`KnowledgePanels.tsx`。
- 每个模块会校验核心可见中文文案存在，覆盖项目入口、项目结构、章节蓝图、任务书、导出、模型调用、本地全文检索、角色卡和已确认事实等入口。
- 更新 `docs/BUILD_AND_RUN.md`，说明 dev smoke 的检查范围。

本轮已单独执行：

```powershell
cd app
npm run smoke:dev
```

结果：35 项 smoke 检查全部通过。

### 18. 收紧 WorkspaceProps 类型边界

本轮处理总控里记录的 `WorkspaceProps` 过宽问题：

- 移除 `WorkspaceProps` 上的 `[key: string]: unknown` 逃生口，让 TypeScript 能检查漏传和拼错的 props。
- 补齐 `Workspace` 实际接收的蓝图历史、作者输入路径、时间线、框架保存/生成、蓝图重生成、作者输入保存等字段和回调类型。
- `recentProjects` 从 `unknown[]` 收窄为 `RecentProject[]`。
- `chapters` 和 `currentChapter` 改为复用真实的 `ChapterSummary` 类型。
- 修复 `ChapterList` 仍读取 `chapter.word_count` 的字段名问题，改为使用后端章节列表实际返回的 `chapter.words`，避免章节列表字数显示为 0。

本轮已重新执行：

```powershell
cd app
npm run verify
```

结果：copy check、前端 build、lint、35 项 dev smoke、13 个 Rust 测试全部通过。

### 19. 修复故事框架面板保存链路

本轮修复故事框架页的主链路问题：

- `FrameworkPanel` 不再把故事前提、角色图谱、世界观、情节大纲等框架文件当作普通模块 Markdown 保存。
- 框架编辑器现在直接绑定 `frameworkContent` 和 `onChangeFrameworkContent`。
- 保存框架文件改为调用专用 `onSaveFrameworkFile`，走后端 `save_framework_file` 流程。
- 框架面板新增“生成草案”入口，直接调用 `onGenerateFrameworkDraft`。
- `saveFrameworkFile` 保存成功后同步回写 `frameworkContent`，并刷新项目 Markdown 文件列表，避免本地文件视图滞后。
- `dev-smoke` 新增框架保存链路契约检查，确认 `CorePanels.tsx` 保留“生成草案”和 `onSaveFrameworkFile` 入口。

本轮已重新执行：

```powershell
cd app
npm run verify
```

结果：copy check、前端 build、lint、37 项 dev smoke、13 个 Rust 测试全部通过。

### 20. 修复时间线面板编辑链路

本轮修复时间线页被误用故事框架状态的问题：

- `timeline` 视图不再复用 `frameworkContent`。
- 时间线编辑器改为读取 `timelineEvents`，路径显示 `timelineEventsPath`。
- 保存时间线改为调用 `onSaveTimelineEvents`，走后端 `save_timeline_events` 流程。
- 编辑内容走 `onChangeTimelineEvents`，避免和故事前提、角色图谱、世界观等框架文件串状态。
- 面板显示当前 Timeline Pro 状态：启用时显示“Timeline Pro 已启用”，否则显示“普通时间线”。
- `dev-smoke` 新增时间线保存链路契约检查，确认 `CorePanels.tsx` 保留 `onSaveTimelineEvents`。

本轮已重新执行：

```powershell
cd app
npm run verify
```

结果：copy check、前端 build、lint、38 项 dev smoke、13 个 Rust 测试全部通过。

### 21. 补全章节蓝图工作台

本轮把蓝图页从单一蓝图编辑器补成完整章节蓝图工作台：

- 蓝图页新增“作者输入”编辑器，直接读写 `authorInput` 和 `authorInputPath`。
- 作者输入保存走 `onSaveAuthorInput`，不再只能在右侧助手里操作。
- 蓝图编辑器保留专用 `onSaveBlueprint` 保存链路。
- 蓝图页新增“生成蓝图草稿”“装配任务书”“重生成后续”“重生成全部”入口。
- 蓝图历史进入主工作区：可查看历史列表、预览历史版本，并恢复到当前编辑器。
- 调整蓝图页布局为章节列表、编辑区、历史区三栏，避免历史面板掉行。
- `dev-smoke` 新增作者输入、蓝图历史、重生成后续入口契约检查。

本轮已重新执行：

```powershell
cd app
npm run verify
```

结果：copy check、前端 build、lint、41 项 dev smoke、13 个 Rust 测试全部通过。

### 22. 修复章节任务书手动保存链路

本轮修复任务页编辑任务书后保存会被后端拒绝的问题：

- `TasksPanel` 已经把章节任务书保存到 `tasks/writing-briefs/{chapter}.md`。
- 后端 `save_module_markdown_file` 过去没有把 `tasks/writing-briefs/*.md` 纳入可编辑辅助 Markdown 白名单，导致保存会被拒绝。
- 现在 `tasks/writing-briefs/*.md` 明确允许通过模块保存链路写入。
- Rust 测试补充任务书手动保存断言，同时继续阻止正文绕过确认流程写入。
- `dev-smoke` 新增 `writingBriefPath` 与 `onSaveModuleMarkdownFile` 契约检查，防止任务书保存入口退回。

本轮已重新执行：

```powershell
cd app
npm run verify
```

结果：copy check、前端 build、lint、43 项 dev smoke、13 个 Rust 测试全部通过。

### 23. 修复角色总览保存链路

本轮修复角色模块里“角色总览”误走通用模块保存的问题：

- 角色总览实际编辑的是 `framework/03-characters.md`，属于故事框架源文件。
- 进入角色总览时，现在会加载专用框架文件 `03-characters.md` 到 `frameworkContent`。
- 角色总览编辑走 `onChangeFrameworkContent`，保存走 `onSaveFrameworkFile`，不再被后端模块保存白名单拒绝。
- 角色模块内点击关系图谱、成长线、角色卡等辅助文件时，仍继续走普通模块 Markdown 保存链路。
- `dev-smoke` 新增角色总览框架保存契约检查，确认 `KnowledgePanels.tsx` 保留 `onSaveFrameworkFile` 和 `onChangeFrameworkContent`。

本轮已重新执行：

```powershell
cd app
npm run verify
```

结果：copy check、前端 build、lint、45 项 dev smoke、13 个 Rust 测试全部通过。

结果：

- `npm run build` 通过。
- `npm run lint` 通过。
- `npm run smoke:dev` 通过，9 项检查全部通过。
- `cargo test` 通过，13 个测试全部通过。
- `npm run verify` 通过，已串起前端构建、lint、smoke 和 Rust 测试。
- `http://localhost:1420` 返回 200。
- 本轮尝试使用 `agent-browser` 做页面自动化验证，但本机当前没有暴露该命令；已先用构建、lint、Rust 单测和 HTTP 可达性覆盖结构调整风险。
- Rust 测试输出当前无 warning。

## 当前主要风险

1. 前端显示文本仍可能有低频历史乱码  
   高频区已清理：主导航、顶部栏、任务栏、右侧助手、工作区主要页面、默认常量和核心 hook 状态消息。后续需要在真实点击流中继续扫低频弹窗、错误分支和样例内容。

2. 浏览器级自动化验证不足  
   已新增无浏览器 smoke 检查和自动 dev server 管理。本机仍未暴露 `agent-browser`，Node 环境也缺少 Playwright，尚未做完整点击流截图验证。

3. `WorkspaceProps` 仍较宽  
   类型已独立到 `workspace/types.ts`。后续如果继续组件化，可按页面收窄 props，减少每个面板能访问的无关字段。

## 建议下一步

优先级从高到低：

1. 做真实点击流验证  
   smoke 只能验证入口资源，不验证 React 交互。后续应覆盖打开样例项目、知识库检索、钉选材料、任务书、候选稿、正文保存。

2. 继续扫低频文案和错误分支  
   用 `rg` 和真实点击流结合检查残留乱码，优先处理用户能看到的消息。

3. 强化 Word 导出和正文排版  
   当前导出已可用，后续需要模板、页眉页脚、章节样式等。

## 当前接力入口

如果继续开发，建议从以下文件开始：

- `app/src/components/Workspace.tsx`
- `app/src/components/workspace/EditorPanels.tsx`
- `app/src/components/workspace/DocumentPanels.tsx`
- `app/src/components/workspace/KnowledgePanels.tsx`
- `app/src/components/workspace/CorePanels.tsx`
- `app/src/components/workspace/types.ts`
- `app/scripts/dev-smoke.mjs`
- `app/scripts/verify.mjs`
- `app/src/App.tsx`
- `app/src/hooks/useProjectResources.ts`
- `app/src/hooks/useChapterWorkspace.ts`
- `app/src-tauri/src/project_model.rs`

如果先做版本管理，建议先运行：

```powershell
git status --short
```

提交前可再次运行：

```powershell
cd app
npm run verify
```
