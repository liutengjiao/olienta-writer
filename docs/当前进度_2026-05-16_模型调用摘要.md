# 当前进度：模型调用摘要

日期：2026-05-16

## 这一步完成

- 新增模型调用历史解析逻辑，从 `logs/model-calls/history.md` 汇总调用次数、失败次数、平均耗时和 Token 总量。
- “模型调用”页在原始 Markdown 历史上方新增摘要栏，进入页面即可看到关键运行指标。
- 平均耗时只统计有效 `durationMs` 样本，失败或缺失耗时的记录不会把平均值压低。
- 扩展逻辑 smoke 和开发 smoke 覆盖，确保摘要字段不会从页面回归消失。

## 涉及文件

- `app/src/lib/modelCallLogic.ts`
- `app/src/components/workspace/DocumentPanels.tsx`
- `app/scripts/logic-smoke.mjs`
- `app/scripts/dev-smoke.mjs`

## 已验证

- `npm run smoke:logic`
- `npm run build`

## 下一步建议

- 继续把模型调用记录做成可筛选列表，支持按 use case、Provider、状态过滤。
- 后续可以把失败调用单独提取为“最近失败”区块，帮助定位 Provider 配置和网络问题。
