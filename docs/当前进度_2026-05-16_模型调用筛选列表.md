# 当前进度：模型调用筛选列表

日期：2026-05-16

## 这一步完成

- `modelCallLogic` 从只产出摘要升级为可解析结构化调用条目。
- “模型调用”页新增筛选浏览器：
  - 按状态筛选：全部、成功、失败、未知。
  - 按调用类型筛选：例如 `candidate-draft`、`provider-test`、`framework-draft`。
  - 支持关键词搜索 Provider、章节、输入输出路径和结果说明。
  - 最近调用倒序展示，最多先展示 12 条，避免历史很长时拖慢页面。
- 调用列表直接展示任务、Provider、状态、章节、耗时、Token、输出路径和结果说明。
- 补充逻辑 smoke，覆盖解析字段和筛选行为。
- 补充 dev smoke，避免模型调用筛选 UI 从页面代码中回归消失。

## 涉及文件

- `app/src/lib/modelCallLogic.ts`
- `app/src/components/workspace/DocumentPanels.tsx`
- `app/src/App.css`
- `app/scripts/logic-smoke.mjs`
- `app/scripts/dev-smoke.mjs`
- `docs/交接盘点_2026-05-16.md`

## 已验证

- `npm run smoke:logic`
- `npm run build`
- `npm run lint`

## 后续可继续

- 为 Provider 增加输入/输出 Token 单价，基于日志估算调用成本。
- 给失败调用做独立聚合区块，按 Provider 和错误原因汇总。
- 在日志中补充模型名、温度、最大输出和超时参数，方便诊断质量和成本。
