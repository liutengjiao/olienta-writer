# 当前进度：Provider 诊断面板

日期：2026-05-17

## 这一步完成

- 模型调用逻辑新增 Provider 级汇总：
  - 调用次数。
  - 失败次数和失败率。
  - 平均耗时。
  - Token 总量。
  - 基于 Provider 单价的预估费用。
  - 覆盖的调用类型。
  - 最近一次调用状态和结果说明。
- “模型调用”页新增“Provider 诊断”面板，用表格方式展示每个 Provider 的稳定性、速度、用量和成本。
- Provider 汇总默认按调用次数排序，失败率直接高亮。
- 移动窄屏下 Provider 诊断表会自动折叠为单列，避免文本挤压。
- 逻辑 smoke 新增 Provider 汇总排序、费用和耗时覆盖。
- dev smoke 新增 Provider 诊断、失败率、最近结果等关键文本覆盖。
- 交接盘点同步更新，标记 Provider 级运营诊断第一版已完成。

## 涉及文件

- `app/src/lib/modelCallLogic.ts`
- `app/src/components/workspace/DocumentPanels.tsx`
- `app/src/App.css`
- `app/scripts/logic-smoke.mjs`
- `app/scripts/dev-smoke.mjs`
- `docs/交接盘点_2026-05-16.md`

## 已验证

- `npm run smoke:logic`
- `npm run build`
- `npm run lint`

## 后续可继续

- 点击 Provider 诊断行后自动筛选下方调用列表。
- 按用途汇总 Provider 表现，例如候选稿、蓝图、框架分别统计失败率和费用。
- 增加错误归因列，把 API Key、配额、超时、网络、返回格式异常区分出来。
