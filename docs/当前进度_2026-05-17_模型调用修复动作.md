# 当前进度：模型调用修复动作

日期：2026-05-17

## 这一步完成

- 失败诊断标题区新增快捷动作：
  - 运行测试
  - Provider 配置
- 单条失败调用的修复建议区也新增同样的快捷动作。
- 新增 `onOpenModelProviders` 回调，用于从模型调用诊断页跳转到结构化 AI Provider 配置页。
- “Provider 配置”跳转复用现有结构化 Provider 页面，避免直接编辑原始 JSON 绕过密钥处理和保存逻辑。
- dev smoke 新增 `Provider 配置` 与 `onOpenModelProviders` 覆盖。
- 交接盘点同步更新，标记修复快捷动作第一版已完成。

## 涉及文件

- `app/src/App.tsx`
- `app/src/components/workspace/types.ts`
- `app/src/components/workspace/DocumentPanels.tsx`
- `app/src/App.css`
- `app/scripts/dev-smoke.mjs`
- `docs/交接盘点_2026-05-16.md`

## 已验证

- `npm run smoke:logic`
- `npm run build`
- `npm run lint`

## 后续可继续

- “运行测试”后自动刷新模型调用记录并定位最新失败。
- 为错误归因卡片增加更具体的动作，例如鉴权直接聚焦 API Key，超时直接聚焦超时秒数。
- 增加一键复制失败摘要，方便粘贴给 Provider 支持或另一个代码工具。
