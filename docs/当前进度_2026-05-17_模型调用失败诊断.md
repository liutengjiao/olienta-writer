# 当前进度：模型调用失败诊断

日期：2026-05-17

## 这一步完成

- 模型调用解析逻辑新增失败聚合：
  - 统计失败总数。
  - 按 Provider 汇总失败次数。
  - 记录每个 Provider 最近失败的任务和原因。
  - 提取最近失败调用，倒序展示。
- “模型调用”页新增“失败诊断”区块：
  - 左侧显示失败 Provider 排行。
  - 右侧显示最近失败。
  - 没有失败时显示空状态。
- 逻辑 smoke 覆盖失败聚合和最近失败原因。
- dev smoke 覆盖失败诊断、失败 Provider、最近失败等页面关键文本。
- 交接盘点同步更新，标记失败聚合诊断第一版已完成。

## 涉及文件

- `app/src/lib/modelCallLogic.ts`
- `app/src/components/workspace/DocumentPanels.tsx`
- `app/src/App.css`
- `app/scripts/logic-smoke.mjs`
- `app/scripts/dev-smoke.mjs`
- `docs/交接盘点_2026-05-16.md`

## 已验证

- `npm run smoke:logic`
- `npm run build`
- `npm run lint`

## 后续可继续

- 对失败原因做规则归因，例如 API Key、配额、网络、超时、JSON 解析、Provider 返回格式异常。
- 在失败卡片上提供“跳到 Provider 配置”和“只看这个 Provider 日志”的快捷动作。
- 做按 Provider / 用途的失败率趋势，而不只是失败次数。
