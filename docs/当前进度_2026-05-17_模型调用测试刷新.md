# 当前进度：模型调用测试刷新

本轮继续推进模型调用诊断链路，把 Provider 测试后的页面状态补齐成闭环。

## 已完成

- `testAiProvider` 完成后会刷新 `logs/model-calls/history.md`，不再误跳到 `tasks/history.jsonl`。
- 模型调用页的“运行测试”按钮会进入测试中状态，避免重复点击。
- 测试完成后自动打开模型调用历史并清空筛选，让列表顶部回到最新记录。
- 如果 Provider 测试失败，页面会自动筛到本次失败 Provider，方便继续修配置。
- Provider 测试卡新增“最新记录”快捷按钮，用于手动清空筛选并查看最新调用。
- 新增刷新提示文案，明确测试完成后已经重新加载模型调用历史。
- dev smoke 增加 `runProviderTestAndRefresh`、`model-call-refresh-note` 和“最新记录”源码契约。

## 验证

- `npm run smoke:logic`：通过，37 checks。
- `npm run build`：通过，仍只有 Vite 大 chunk 体积提示。

## 后续建议

- 让后端 `test_ai_provider` 返回写入的模型调用记录 ID，前端可直接高亮该条记录。
- 给失败记录增加“一键带入 Provider 配置”的定位，例如按 provider id 展开对应配置卡。
