# 当前进度：模型调用钻取筛选

日期：2026-05-17

## 这一步完成

- 模型调用筛选逻辑新增 Provider 精确筛选。
- “Provider 诊断”行现在可点击，点击后自动筛选下方调用记录为该 Provider。
- “失败 Provider”卡片现在可点击，点击后自动筛选为该 Provider 的失败调用。
- 调用记录筛选栏新增 Provider 下拉框。
- 调用记录筛选栏新增快捷动作：
  - 只看失败。
  - 清除筛选。
- 当前选中的 Provider 诊断行会高亮。
- 逻辑 smoke 新增 Provider 精确筛选覆盖。
- dev smoke 新增 `providerFilter`、只看失败、清除筛选覆盖。

## 涉及文件

- `app/src/lib/modelCallLogic.ts`
- `app/src/components/workspace/DocumentPanels.tsx`
- `app/src/App.css`
- `app/scripts/logic-smoke.mjs`
- `app/scripts/dev-smoke.mjs`
- `docs/交接盘点_2026-05-16.md`

## 已验证

- `npm run smoke:logic`
- `npm run build`
- `npm run lint`

## 后续可继续

- 点击 Provider 后自动滚动到调用记录列表。
- 在 Provider 诊断里增加按用途钻取，例如只看候选稿生成或只看蓝图生成。
- 给错误原因加归因标签，再支持按错误类型筛选。
