# 当前进度：模型调用错误归因

日期：2026-05-17

## 这一步完成

- 模型调用逻辑新增失败分类器：
  - 鉴权
  - 配额
  - 限流
  - 超时
  - 网络
  - 返回格式
  - Provider
  - 未知
- 失败诊断区新增“错误归因”列，按错误类型汇总失败次数和最近消息。
- 点击错误归因卡片后，会自动筛选下方调用记录为该错误类型的失败调用。
- 调用记录筛选栏新增“错误类型”下拉框。
- 单条调用记录新增“错误”字段，失败时显示归因标签。
- 逻辑 smoke 新增错误类型筛选、鉴权识别和错误归因汇总覆盖。
- dev smoke 新增错误归因、错误类型、`failureKindFilter`、`formatFailureKind` 覆盖。
- 交接盘点同步更新，标记错误归因第一版已完成。

## 涉及文件

- `app/src/lib/modelCallLogic.ts`
- `app/src/components/workspace/DocumentPanels.tsx`
- `app/src/App.css`
- `app/scripts/logic-smoke.mjs`
- `app/scripts/dev-smoke.mjs`
- `docs/交接盘点_2026-05-16.md`

## 已验证

- `npm run smoke:logic`
- `npm run build`
- `npm run lint`

## 后续可继续

- 增加更细的 Provider 返回码解析，例如区分模型不存在、Base URL 错误、余额不足。
- 后端写日志时增加 `errorCode` 或 `errorKind` 字段，减少前端从文本猜测。
- 给错误归因卡片增加修复建议，例如“检查 API Key”“降低并发”“增大超时时间”。
